/**
 * Feature-flag da Cantina.
 *
 * A feature está pronta (catálogo, carteira, balcão, preparo) mas ocultada por
 * enquanto — prioridades mudaram. Todo o código permanece no repositório; para
 * reativar tudo (nav do admin + rotas públicas e admin), basta trocar para `true`.
 */
export const CANTINA_ENABLED = false;
